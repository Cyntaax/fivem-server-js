import { FC } from "react";
import { Button, ThemeWrapper, Box } from "retro-ui";

const App: FC = () => {
	return (
		<ThemeWrapper>
			<div className="loader-main">
				<h1>Loading...</h1>
			</div>
		</ThemeWrapper>
	);
};

export default App;
